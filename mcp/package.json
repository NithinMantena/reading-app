{
  "name": "reading-app-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Docker and local MCP tools for the Reading app API",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node index.mjs",
    "setup": "node setup.mjs",
    "check": "node check.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0"
  }
}
